const intialState= {
    products:[{
        id: 1,
        title: "shivam",
        category: "coder",
    },],
};
export const productReducer=(state,{type,payload})=>{
    switch(action.type){
        case SET_PRODUCTS:
            return state;

        default:
            return state;
    }
}